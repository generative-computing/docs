---
title: mellea.backends.process_reward_models.huggingface.prms
description: PRM Implementations for Local HuggingFace Backends.
sidebarTitle: prms
---




## Classes
---

### <span className="ml-2 inline-flex items-center rounded-full px-2 py-1 text-[0.7rem] font-bold tracking-wide bg-[#4ADE8033]/20 text-[#15803D]">CLASS</span> `HFGenerativePRM`
---


A Generative PRM that works with a huggingface backend.


**Methods:**

#### <span className="ml-2 inline-flex items-center rounded-full px-2 py-1 text-[0.7rem] font-bold tracking-wide bg-[#3064E3]/20 text-[#1D4ED8]">FUNC</span> `score`
---

```python
score(self, query: str, response: str) -> tuple[list[float], list[list[float]]]
```

Returns a final and per-step score for a given input query and response.

**Args:**
- `query`: User query
- `response`: Assistant Response to score


#### <span className="ml-2 inline-flex items-center rounded-full px-2 py-1 text-[0.7rem] font-bold tracking-wide bg-[#3064E3]/20 text-[#1D4ED8]">FUNC</span> `prepare_inputs`
---

```python
prepare_inputs(self, user_content: str, steps: list[str]) -> BatchEncoding
```

Prepare the inputs for inference with the model.

**Args:**
- `user_content`: the user query
- `steps`: assistant response, broken down into steps


### <span className="ml-2 inline-flex items-center rounded-full px-2 py-1 text-[0.7rem] font-bold tracking-wide bg-[#4ADE8033]/20 text-[#15803D]">CLASS</span> `HFRegressionPRM`
---


A Regression PRM that works with a huggingface backend.


**Methods:**

#### <span className="ml-2 inline-flex items-center rounded-full px-2 py-1 text-[0.7rem] font-bold tracking-wide bg-[#3064E3]/20 text-[#1D4ED8]">FUNC</span> `score`
---

```python
score(self, query: str, response: str) -> tuple[list[float], list[list[float]]]
```

Returns a final and per-step score for a given input query and response.

**Args:**
- `query`: User query
- `response`: Assistant Response to score


#### <span className="ml-2 inline-flex items-center rounded-full px-2 py-1 text-[0.7rem] font-bold tracking-wide bg-[#3064E3]/20 text-[#1D4ED8]">FUNC</span> `prepare_inputs`
---

```python
prepare_inputs(self, user_content: str, steps: list[str]) -> BatchEncoding
```

Prepare the inputs for inference with the model.

**Args:**
- `user_content`: the user query
- `steps`: assistant response, broken down into steps

---